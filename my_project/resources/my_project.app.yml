resources:
  apps:
    hello_world_app:
      name: hello-world-app
      description: "Hello World FastAPI + React application deployed on Databricks"
      source_code_path: ../src
      config:
        command:
          - "python"
          - "-m"
          - "uvicorn"
          - "my_project.main:app"
          - "--host"
          - "0.0.0.0"
          - "--port"
          - "8080"

